package chapter05;

public class SelectionSort {
	public static void main(String[] args) {
		int[] arr = new int[5];
		for(int i = 0; i < arr.length; i++) {
			if (arr[i - 1] < arr[i]) {
				arr[i  - 1] = arr[i];
				arr[i] = arr[i - 1];
			}
		}
	}
}
